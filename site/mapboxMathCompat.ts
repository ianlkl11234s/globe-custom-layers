// Existing scene modules use only this coordinate helper; no Mapbox SDK is
// bundled or initialized by the MapLibre build.
import { MercatorCoordinate } from 'maplibre-gl';
export default { MercatorCoordinate };
