{
  "name": "00-native-vs-custom",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mapbox GL JS: the same airport points drawn two ways -- a native `circle` layer vs. a Three.js CustomLayerInterface -- switchable side by side, to show what a custom layer costs you and what it gives up.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mapbox-gl": "^3.18.1",
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "typescript": "~5.7.0",
    "vite": "^6.1.0",
    "vitest": "^3.2.6"
  }
}
