{
  "name": "mass-trajectories",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mapbox GL JS + Three.js: thousands of independently-moving flight-like trails batched into a single THREE.Line draw call, with guard-vertex slot isolation, partial buffer uploads, a hybrid precomputed/shader-derived ECEF split, and a min-heap eviction scheme for when demand outstrips slot capacity -- the 'batched trails' scaling recipe.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vitest bench --run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mapbox-gl": "3.30.0",
    "three": "0.172.0"
  },
  "devDependencies": {
    "@types/geojson": "7946.0.16",
    "@types/three": "0.172.0",
    "typescript": "5.7.3",
    "vite": "6.4.3",
    "vitest": "3.2.7"
  }
}
