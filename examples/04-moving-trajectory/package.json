{
  "name": "moving-trajectory",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mapbox GL JS + Three.js: 30 objects animate along great-circle routes with fading ring-buffer trails, deriving ECEF from mercator in the vertex shader every frame instead of precomputing it -- the 'geometry moves' branch of the globe-hugging recipe.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mapbox-gl": "^3.18.1",
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "typescript": "~5.7.0",
    "vite": "^6.1.0",
    "vitest": "^3.2.6"
  }
}
