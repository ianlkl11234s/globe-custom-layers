{
  "name": "02-arcs-on-globe",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mapbox GL JS + Three.js: great-circle O-D arcs between airport hubs, hugging the globe projection -- and a segments-per-arc slider that shows exactly why an under-subdivided arc cuts through the planet.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mapbox-gl": "^3.18.1",
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "typescript": "~5.7.0",
    "vite": "^6.1.0",
    "vitest": "^3.2.6"
  }
}
