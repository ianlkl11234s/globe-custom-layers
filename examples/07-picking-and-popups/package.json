{
  "name": "07-picking-and-popups",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mapbox GL JS + Three.js: custom layers don't hit-test -- two working strategies (a companion native layer, and screen-space projection with a manual globe backface test) for making one clickable anyway, plus why a GPU ID buffer is the third option this example doesn't implement.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mapbox-gl": "^3.18.1",
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "typescript": "~5.7.0",
    "vite": "^6.1.0",
    "vitest": "^3.2.6"
  }
}
