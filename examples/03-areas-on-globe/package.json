{
  "name": "03-areas-on-globe",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mapbox GL JS + Three.js: filled polygons and geodesic circles that hug the globe projection, showing why straight edges are chords and why a fixed-radius circle isn't round on screen.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mapbox-gl": "3.30.0",
    "three": "0.172.0"
  },
  "devDependencies": {
    "@types/geojson": "7946.0.16",
    "@types/three": "0.172.0",
    "typescript": "5.7.3",
    "vite": "6.4.3",
    "vitest": "3.2.7"
  }
}
